define([
    './Module'
], function(module) {
    return module.filter('parentFolder', function() {
        return function(items, numLvl) {
            var filtered = [];
            
            return filtered;
        };
    });
});
